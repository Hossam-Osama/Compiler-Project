First(CLASS_BODY) = { ASSIGNMENT DECLARATION Epsilon METHOD_LIST }
First(CLASS_DECL) = { MODIFIER }
First(DECLARATION ) = { PRIMITIVE_TYPE }
First(EXPRESSION ) = { ( EXPRESSION IDENTIFIER NUMBER }
First(IF ) = { if }
First(METHOD_BODY) = { STATEMENT_LIST }
First(METHOD_DECL) = { MODIFIER }
First(METHOD_LIST) = { METHOD_DECL METHOD_LIST }
First(MODIFIER ) = { private protected public }
First(PRIMITIVE_TYPE ) = { float int }
First(STATEMENT_LIST) = { STATEMENT STATEMENT_LIST }
First(STATMENT ) = { ASSIGNMENT DECLARATION IF WHILE }
First(WHILE ) = { while }
Follow(CLASS_BODY) = { $ }
