First(DECLARATION ) = { PRIMITIVE_TYPE }
First(EXPRESSION ) = { SIMPLE_EXPRESSION }
First(FACTOR ) = { ( id num }
First(IF ) = { if }
First(METHOD_BODY ) = { STATEMENT_LIST }
First(PRIMITIVE_TYPE ) = { float int }
First(SIGN ) = { + - }
First(SIMPLE_EXPRESSION ) = { SIGN SIMPLE_EXPRESSION TERM }
First(STATEMENT ) = { ASSIGNMENT DECLARATION IF WHILE }
First(STATEMENT_LIST ) = { STATEMENT STATEMENT_LIST }
First(TERM ) = { FACTOR TERM }
First(WHILE ) = { while }
Follow(DECLARATION ) = { $ }
